# Benchmark runners

Wrapper scripts that execute the benchmark suites in `../benchmarks/` and
produce summary reports. Populated in Phase 6.

Planned scripts:

- `run_performance.jl` — regression check on timing across releases.
- `run_triangulation.jl` — Stan/NIMBLE cross-validation pipeline.
- `run_books.jl` — textbook reproduction suite.
